<h1><%= @tasks.title %></h1>
<h2><%= link_to "Edit this task", edit_path %></h2>
<%= @tasks.details %>
<input type="checkbox" <%= @tasks.completed ? 'checked' : '' %>>

<%= link_to "Delete",
  task_path(@tasks.id),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
  %>


<%= link_to "Back to list", tasks_path %>
